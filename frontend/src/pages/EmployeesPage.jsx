import { Typography } from '@mui/material'

/** Employees list. Built out in the next step (table + filters + search). */
export default function EmployeesPage() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Employees
      </Typography>
      <Typography variant="body2" color="text.secondary">
        The searchable, filterable employee table is coming next.
      </Typography>
    </>
  )
}
